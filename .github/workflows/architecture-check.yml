name: アーキテクチャチェック

on:
  pull_request:
    branches:
      - main

jobs:
  architecture-check:
    name: レイヤードアーキテクチャチェック
    runs-on: ubuntu-latest

    steps:
      - name: チェックアウト
        uses: actions/checkout@v7

      - name: アーキテクチャ違反チェック実行
        run: bash scripts/check-architecture.sh backend
