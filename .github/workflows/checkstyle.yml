name: Javadocチェック

on:
  pull_request:
    branches:
      - main

jobs:
  checkstyle:
    name: Javadocチェック
    runs-on: ubuntu-latest

    steps:
      - name: チェックアウト
        uses: actions/checkout@v4

      - name: JDK 21 セットアップ
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Gradleキャッシュ
        uses: actions/cache@v6
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Javadocチェック実行
        run: ./backend/gradlew -p backend checkstyleMain

      - name: チェック結果アップロード
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: checkstyle-report
          path: backend/build/reports/checkstyle/
